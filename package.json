{
  "name": "quickstart",
  "version": "1.0.0",
  "private": true,
  "description": "A step-by-step guide: set up GitHub, run an AI coding agent, make your own version of a starter site and publish it with GitHub Pages.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "og": "node scripts/og.mjs"
  },
  "license": "MIT"
}
